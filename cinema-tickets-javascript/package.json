{
  "name": "cinema-tickets-javascript",
  "description": "This is a coding exercise which will allow you to demonstrate how you code and your approach to a given problem.",
  "version": "1.0.1",
  "engines": {
    "node": ">=20.9.0"
  },
  "type": "module",
  "scripts": {
    "test": "vitest run **/*.spec.js",
    "test:watch": "vitest --watch **/*.spec.js",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "colors": "^1.4.0",
    "dotenv": "^17.2.0",
    "pino": "^9.7.0",
    "prettier": "^3.6.2",
    "vitest": "^3.2.4"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "pino-pretty": "^13.0.0"
  }
}
